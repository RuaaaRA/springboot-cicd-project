name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: ✅ Checkout Code
      uses: actions/checkout@v3

    - name: ☕ Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: 🛠️ Make mvnw executable
      run: chmod +x mvnw

    - name: 🧪 Build & Test with Maven
      run: ./mvnw clean verify

    - name: 📦 Build Docker Image
      run: docker build -t springboot-app .

    # هذه الخطوة ستكون مفعلة لاحقًا عند تجهيز Docker Hub
    # - name: 🚀 Push to Docker Hub
    #   env:
    #     DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
    #     DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
    #   run: |
    #     echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
    #     docker tag springboot-app $DOCKER_USERNAME/springboot-app:latest
    #     docker push $DOCKER_USERNAME/springboot-app:latest
